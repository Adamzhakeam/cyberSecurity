// Frontend configuration entrypoint.
// Edit `window.API_BASE` here for production deployments (or set an
// inline script in your HTML to override before other scripts load).
// Example (Netlify):
// <script>window.API_BASE = "https://your-backend.onrender.com/api";</script>

window.API_BASE = window.API_BASE || null;
