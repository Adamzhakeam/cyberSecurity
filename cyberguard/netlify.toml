[build]
  publish = "frontend"
  command = ""

[context.production.environment]
# Optionally set API_BASE at build time by replacing this value, or
# leave empty and set runtime `window.API_BASE` in the HTML if needed.
# API_BASE = "https://your-backend.onrender.com/api"
